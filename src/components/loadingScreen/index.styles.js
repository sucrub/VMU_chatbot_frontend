import { COLOR } from "../../configs/color";

const styles = {
    container: {
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '100vh',
        background: COLOR.menuBg,
    },
    spin: {
        transform: 'scale(2)'
    }
}

export default styles;